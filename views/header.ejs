<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= "EverythingLearn" %></title>
    <!-- External CSS Libraries -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.min.css">
    <!-- External JS Libraries -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/wow/1.1.2/wow.min.js"></script>
    <style>
        /* Navbar Styling */
        .navbar {
            background-color: #1f1f1f;
            color: #fff;
            padding: 20px 50px;
        }
        .navbar-brand h2 {
            font-family: 'Arial', sans-serif;
            font-size: 30px;
            color: #fff;
            letter-spacing: 2px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .navbar-nav .nav-link {
            color: #fff;
            font-size: 18px;
            font-weight: 500;
            padding: 10px 15px;
            margin: 0 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: color 0.3s ease, padding 0.3s ease;
        }
        .navbar-nav .nav-link:hover {
            color: #FFD700;
            padding-bottom: 12px;
            border-bottom: 2px solid #FFD700;
        }
        .btn-brown {
            background-color: #8B4513;
            color: white;
            border-radius: 30px;
            font-size: 16px;
            font-weight: 600;
            padding: 10px 20px;
            transition: background-color 0.3s ease;
        }
        .btn-brown:hover {
            background-color: #6f3d0b;
        }
        .btn-outline-brown {
            border: 2px solid #8B4513;
            color: #8B4513;
            border-radius: 30px;
            font-size: 16px;
            font-weight: 600;
            padding: 10px 20px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .btn-outline-brown:hover {
            background-color: #8B4513;
            color: white;
        }
        .navbar-nav {
            transition: transform 0.3s ease-in-out;
        }
        .navbar-toggler {
            border-color: #8B4513;
        }
        .navbar-toggler-icon {
            background-color: #8B4513;
        }
        .navbar.sticky-top {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        @media (max-width: 768px) {
            .navbar-nav {
                text-align: center;
                margin-top: 15px;
            }
            .navbar-nav .nav-link {
                padding: 12px 25px;
                margin: 0;
                display: inline-block;
            }
        }
    </style>
</head>
<body>
<!-- Navbar Start -->
<nav class="navbar navbar-expand-lg navbar-dark sticky-top">
    <a href="/" class="navbar-brand d-flex align-items-center px-4 px-lg-5">
        <h2 class="m-0">
            <i class="fa-solid fa-book-open me-3"></i>EverythingLearn
        </h2>
    </a>
    <!-- Toggle Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav ms-auto p-4 p-lg-0">
            <a href="/" class="nav-item nav-link active">Home</a>
            <a href="/about" class="nav-item nav-link">About</a>
            <a href="/main" class="nav-item nav-link">Courses</a>
            <a href="/contact" class="nav-item nav-link">Contact</a>
        </div>
        <div class="d-flex">
            <% if (!locals.user) { %>
                <!-- User Not Logged In: Show Login & Register -->
                <a href="/login" class="btn btn-outline-brown py-2 px-4 mx-2 d-none d-lg-block">Login</a>
                <a href="/register" class="btn btn-brown py-2 px-4 mx-2 d-none d-lg-block">Join Now <i class="fa fa-arrow-right ms-3"></i></a>
            <% } else { %>
                <!-- User Logged In: Show Profile & Logout -->
                <a href="/profile" class="btn btn-outline-brown py-2 px-4 mx-2 d-none d-lg-block">My Profile</a>
                <a href="/logout" class="btn btn-brown py-2 px-4 mx-2 d-none d-lg-block">Logout</a>

                <!-- Show Upgrade Button for Free or Pro Subscription -->
                <% if (locals.subscription_type === 'Free' || locals.subscription_type === 'Pro') { %>
                    <a href="/upgrade" class="btn btn-outline-brown py-2 px-4 mx-2 d-none d-lg-block">Upgrade</a>
                <% } %>
            <% } %>
        </div>
    </div>
</nav>
<!-- Navbar End -->
</body>
</html>
